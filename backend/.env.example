# ============================================================
# Données sensibles — copier ce fichier en « .env » (même dossier)
# et y mettre les vraies valeurs. Le .env n'est JAMAIS versionné.
# ============================================================

# Connexion PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/nfctag
DB_USER=nfctag
DB_PASSWORD=change-me
